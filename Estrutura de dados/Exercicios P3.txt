1 2 3 envolvendo recursividade 
4 ia pra exercicio de a até b envolvendo Arvores AVL

1 - não lembro ao certo mas era pra somar os elementos de um algarismo, exemplo, 132
somar esses 3 elementos e usar recursão.

2 - era pra achar o maior elemento dentro de uma arvore estatica, alguma coisa assim não lembro direito

3 - somar os elementos de uma arvore, so fazer
if(lista == NULL)
{
	return 0;
}
else
{
return raiz->info + nomedafunção(raiz->esq) + nomedafunção(raiz->dir);
}

4 - era envolvendo arvore AVL, esquema de inserir nós e equilibrar ela, pelo jeito ele não desconsidera tudo se caso vc tenha feito errado
mas tenta fazer tudo na medida do possivel ao seu alcance.
